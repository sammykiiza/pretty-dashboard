import React from 'react'
// import { api } from '../../../Core/Services/api'

function Card() {
    // const { data = [] } = api.useCoinsDetailsQuery('bitcoin');

  return (
    <div>Card</div>
  )
}

export default Card